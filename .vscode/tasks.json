{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "gcc -Wall -fPIC -shared src/game.c -o src/libgame.so && gcc -w src/game_engine.c   -I. libs/libSDL3.a  -lm -lGLESv2 -o build/game.out && ./build/game.out ",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
