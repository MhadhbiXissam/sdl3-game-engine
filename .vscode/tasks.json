{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "gcc -w ${file}  -I. libs/libSDL3.a  -lm -lGLESv2 -o build/${fileBasenameNoExtension}.out && ./build/${fileBasenameNoExtension}.out ",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
